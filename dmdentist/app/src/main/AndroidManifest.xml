<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.test.dmdentist">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ViewPresiptionActivity"></activity>
        <activity android:name=".ViewPatient" />
        <activity android:name=".PatientAdd" />
        <activity android:name=".PatientManage" />
        <activity android:name=".ManageRecords" />
        <activity android:name=".CreateProfileActivity" />
        <activity android:name=".ClinicActivity" />
        <activity android:name=".LogoutActivity" />
        <activity android:name=".ForgetPassword" />
        <activity android:name=".Dashboard" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>